#!/usr/bin/env thue
# this should work as a comment and not make a big error and
# the first line should also not cause an error because it's defining
# how this file should be run as an executable.

a::=~this file tests comments and executable labels.
::=
a
